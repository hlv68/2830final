1、Web应用程序必须使用HTML 5和CSS的网页内容和布局。页面必须正确使用HTML 5规范格式。表不可用于一般内容布局。表只能用于表格信息。CSS必须用于造型的内容和做的视觉布局。一个HTML 5文档类型是必需的。

2、组成网络应用程序的页面/部分必须有一个统一的设计/界面。应该有每个页面的元素/部分，它们共享共同的，如标题，菜单，尾等，你有灵活性，你如何实现你的设计，但它不应该只是一个随机的页面/部分设计。用户在从页面到页面、节段、或应用程序功能时，应具有一致的和可理解的经验。


3、Web应用程序必须很好的­‐结构和逻辑组织。对页面设计中常见元素的更改必须易于实现。在多个（或所有）页/部分，如标题、菜单和/或脚注中重复的元素是一个元素。如果一个普通的元素必须在多个地方编辑它，那么你的执行是不正确的

4、Web应用程序必须有内容或功能，公开的内容或功能，才可以访问，如果认证（登录­‐在）。当一个用户登录­‐他们必须有一些视觉线索，表明他们登录­‐他们必须有一些视觉线索，表明他们在登录­‐。登出的能力必须是可用的。用户登录后，或如果他们不登录，他们就不能访问受保护的页面或功能。

5、为了测试登录和访问受保护的内容，下面的用户名和密码必须工作为一般访问受保护的内容：用户身份证：测试密码：通过如果您需要实现一个单独的登录管理功能或不同类别的用户，然后提供必要的登录凭据进行登录。

6、Web应用程序必须使用PHP。

7、你必须正确地使用和张贴。使用获取的私人信息是不可接受的。不应该使用不采取行动，如删除信息或提交密码。记住把信息放在网址里！

8、网络应用程序必须使用表单元素，超出登录表单所需的形式。


9、任何地方，用户可以提供输入，您必须提供适当的和信息反馈，如果输入的信息是不完整或正确的。例如，如果用户提供登录信息不正确的他们应该得到反馈，反馈的登录，登录失败…不只是重新提出一个没有空信息登录表单­‐。

10、网络应用程序必须包含一个页面，在页面上有多张照片。

11、Web应用程序必须包含一个页面包含一个YouTube视频嵌入在网页或其他。

12、Web应用程序必须使用JavaScript。

13、Web应用程序必须使用jQuery。

14、Web应用程序必须使用jQuery UI或启动界面元素。

15、Web应用程序必须使用Ajax。AJAX可以利用JavaScript jQuery或功能实现。

16、您所构建的网络应用程序是不平凡的，简单地在本文档中设置的技术要求。是的，你是符合要求的，但你也是建立一个有目的的网络应用程序，并提供功能或能力。的要求是在设计和实施的应用程序所使用的参数，他们不打算是最终产品。你应该建立一个网络应用程序，你会很乐意显示给未来的雇主或客户。你还应该确保你能完成这个发展的日期。

17、一个文档是一个提供连接到网络应用程序的文件，描述了你如何满足本文档中提供的标准。你应该能够在你的网络应用程序的实例，在那里的标准已经满足。这个文件应该像你肩上的学生说，“是的，那边是我使用jQuery。”明确你是如何认识学生/教师可以肯定他们是不是遗漏了什么标准。它也将节省时间的年级，这是供应短缺在期末考试周。很容易找到的东西=使年级快乐=好成绩。

18、您将提供该文档（见17），该文档包含链接到网络应用程序，它描述了如何在压缩文件中满足您网站的所有文件的副本。最后一个项目的提交是通过最后的项目是通过在作业列表中的最后一个项目分配。将压缩文件附加到您提交的类网站上。在文档中的网址必须指向您的网络应用程序的操作版本